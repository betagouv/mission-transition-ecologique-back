# PayloadCMS
PAYLOAD_SECRET=your-secret-here

# Database (SQLite — fichier local, pas de serveur requis)
# DATABASE_URI=file:./tee-poc.db  ← valeur par défaut, variable optionnelle
DATABASE_URI=file:./tee-poc.db

# API AGIR — base URL publique utilisée pour construire les liens de l'index
# (urlDetail / urlPivot). Défaut : http://localhost:3000
# AGIR_PUBLIC_BASE_URL=https://entreprises.ademe.fr
